package com.ishvatov.validation.driver;

/**
 * Empty marker interface, which defines a new group of
 * checks, which contains the following checks: {@link NotExistingDriverId}.
 *
 * @author Sergey Khvatov
 */
public interface NotExistingDriverChecks {
}
